<h1>Mes tasks</h1>

<% @tasks.each do |task| %>
    <h2><%= task.name %><h2>
    <p><%= task.details %></p>
    <p><%= link_to "More details", task_path(task) %></p>
    <%= link_to "Remove task", task_path(task), method: :delete%>
<% end %>
